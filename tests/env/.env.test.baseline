# Baseline test profile — all cloud-sync flags off, behaves like Stage 0
# (Dexie-only). Used to verify "flag 默认关 = 字节级一致" — backend code on
# disk but no runtime requests to 9011.
DEXIE_DB_URL=
BACKEND_DATA_API_URL=
BACKEND_AUTH=
BACKEND_DATA_TABLES=
REALTIME_TRANSPORT=
EXPOSE_DB=true
