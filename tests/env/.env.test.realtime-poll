# realtime-poll profile — Stage 2 / Step 5 poll 降级路径。providers 走 backend
# REST + 5s 间隔的 GET ?since= 轮询。双 tab 5s 内最终一致。
DEXIE_DB_URL=
BACKEND_DATA_API_URL=http://127.0.0.1:9011
BACKEND_AUTH=true
BACKEND_DATA_TABLES=providers,reactives
REALTIME_TRANSPORT=poll
EXPOSE_DB=true
