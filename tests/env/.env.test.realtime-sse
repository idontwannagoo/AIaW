# realtime-sse profile — Stage 2 / Step 5 SSE 降级路径。providers 走 backend
# REST + SSE 单向流。双 tab 联动（延迟略高于 WS）。
DEXIE_DB_URL=
BACKEND_DATA_API_URL=http://127.0.0.1:9011
BACKEND_AUTH=true
BACKEND_DATA_TABLES=providers,reactives
REALTIME_TRANSPORT=sse
EXPOSE_DB=true
